{
  "name": "signalhub-desktop",
  "version": "0.1.1",
  "private": true,
  "description": "SignalHub Electron desktop shell",
  "main": "src/main.js",
  "author": "SignalHub Team",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "concurrently -k -n VITE,ELECTRON -c \"magenta,cyan\" \"npm --prefix ../frontend run dev\" \"wait-on tcp:127.0.0.1:3000 && electron .\"",
    "start": "electron .",
    "pack": "npm --prefix ../frontend run build:electron && npm run prepack:check && electron-builder --dir -c electron-builder.yml",
    "prepack:check": "bash ../scripts/check-prepack.sh",
    "dist": "npm --prefix ../frontend run build:electron && npm run prepack:check && electron-builder -c electron-builder.yml"
  },
  "dependencies": {
    "semver": "^7.6.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^31.3.1",
    "electron-builder": "^24.13.3",
    "wait-on": "^7.2.0"
  }
}
